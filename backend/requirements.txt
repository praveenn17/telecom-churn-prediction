# ── Churn Prediction FastAPI Backend ── requirements.txt ──────────────────────
# Python 3.11 required
# Install with: pip install -r backend/requirements.txt
#
# NOTE: scikit-learn, xgboost, joblib versions must match what was used to
# train and save ml/model.pkl — mismatches can cause joblib deserialization
# errors. Keep them in sync with ml/requirements.txt.

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
pandas==2.2.3
numpy==2.1.0
scikit-learn==1.5.2
xgboost==2.1.1
joblib==1.4.2
python-multipart==0.0.9
